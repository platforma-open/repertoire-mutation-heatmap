# repertoire-mutation-heatmap

**Mutation Explorer** — a Platforma block that shows every measured metric per mutation instead of
per variant, on a position × residue map.

The npm package identifier stays `repertoire-mutation-heatmap`; "Mutation Explorer" is the name the
block shows in the desktop app, matching the Mutation Explorer block in the
precision-synthetic-library-analysis umbrella spec.
